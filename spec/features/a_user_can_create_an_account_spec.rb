require 'rails_helper'

describe "User can create an account" do
  scenario "a user can put in information" do
    skip
    user = create(:user)

    visit
  end

  scenario "a user can be an admin" do
    skip
  end
end
